## Personalize the cover headline

In `src/routes/playbook.index.tsx`, change the H1 leading word from **"The"** to **"Your"** so it reads:

> Your *Dispute* Playbook.

No other copy, styling, or layout changes — gradient italic on "Dispute" and overall structure stay exactly as they are.

### File touched
- `src/routes/playbook.index.tsx` — single word swap inside the `<h1>`.

Once you approve, I'll also follow up on the still-open headline question (adding "made interactive" + which dark-blue shade) — that decision is independent of this rename.